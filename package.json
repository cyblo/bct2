{
  "name": "mpa",
  "version": "1.0.0",
  "description": "Medical Policy Automation - Blockchain + SSI System",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.27.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
